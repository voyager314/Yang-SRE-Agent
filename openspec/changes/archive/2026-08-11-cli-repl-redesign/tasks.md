# Tasks: CLI REPL Redesign

- [x] T1: Rewrite `cli.py` — REPL loop and entry point. Replace the three Typer subcommands with a single main command. Remove `ask()`, `chat()`, `toolset_list()` commands. Add optional positional `question` argument. Add `-p`/`--print` flag for non-interactive mode. Implement `_repl_loop()`: prompt → dispatch → investigate → loop. `-p` mode: wrap question in investigate template, run once, print, exit. Default mode: if `question` provided, investigate first then enter loop; otherwise enter loop directly. Keep `_build_engine()` and `_render_stream()` largely unchanged. Files: `src/sre_agent/cli.py`
- [x] T2: Implement slash command dispatch. Implement `_dispatch_slash(cmd, args, ...)` function. Parse input: split on first space → command name + rest as args. Route to handler functions. Unknown command: print error, continue loop. Files: `src/sre_agent/cli.py`
- [x] T3: Implement `/new` command. Reset `messages` to `[system_prompt]`. Call `evidence_store.clear()` (add method if missing). Call `scratchpad.clear()` (add method if missing). Print confirmation. Files: `src/sre_agent/cli.py`, `src/sre_agent/core/evidence_store.py`, `src/sre_agent/core/scratchpad.py`
- [x] T4: Implement `/model` command. `/model` (no args): show current model + registry listing. `/model <name>`: resolve via `config.resolve_model(name)`, update LLM fields in-place. Track current model registry key for display. Print confirmation with model name and identifier. Files: `src/sre_agent/cli.py`
- [x] T5: Implement `/toolset`, `/help`, `/exit`. `/toolset`: reuse display logic from old `toolset_list`. `/help`: print command table. `/exit` / `/quit`: print goodbye, break loop. Files: `src/sre_agent/cli.py`
- [x] T6: Add `clear()` methods to EvidenceStore and Scratchpad. `EvidenceStore.clear()`: reset internal store dict. `Scratchpad.clear()`: reset entries (verify if already exists). Files: `src/sre_agent/core/evidence_store.py`, `src/sre_agent/core/scratchpad.py`
- [x] T7: Update tests. Update or replace existing CLI tests for `ask`, `chat`, `toolset`. Add tests for REPL slash command dispatch. Add tests for model switching (LLM fields updated, messages preserved). Add tests for `/new` (state cleared). Files: `tests/`
