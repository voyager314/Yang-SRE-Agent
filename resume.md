# SRE Agent — 简历项目描述

## 项目一句话

独立设计并实现了一个 AI 驱动的 SRE 故障诊断 Agent，集成 Prometheus/Loki/Jaeger/AlertManager 四大可观测性数据源，通过 ReAct 工具调用循环、三层上下文压缩和跨会话语义记忆实现自主诊断，在 5 个生产级故障场景评测中根因定位准确率 100%、综合评分 0.91。

---

## 简历条目（分点版）

**SRE Agent — AI 驱动的基础设施故障自主诊断系统** ｜ 独立开发 ｜ Python

- 设计并实现了基于 LLM Function Calling 的 ReAct 诊断引擎，支持多步工具调用、流式输出和自动收敛；引擎在单次调查中协调 8–60+ 次工具调用，覆盖从告警发现到根因定位的完整链路
- 构建三层上下文管理机制（Scratchpad 结构化假设追踪 + 即时/批量压缩 + EvidenceStore 证据落盘回取），基于 token 预算动态决策压缩或收敛，使 Agent 在长链路调查中不超出模型上下文窗口
- 实现基于 ChromaDB + sentence-transformers (GTE-Qwen2-1.5B) 的跨会话语义记忆系统，支持调查摘要的 LLM 提取、向量化持久存储和相似度检索，使 Agent 能复用历史诊断经验
- 集成 Prometheus（即时/范围查询）、Loki（日志检索）、Jaeger/Tempo（分布式链路追踪）、AlertManager（告警/静默查询）四大可观测性工具集，每个工具集实现独立的语义压缩策略
- 设计并实现 5 维度混合评测体系（根因定位 30% + 信号覆盖 15% + 推理质量 25% + 工具效率 10% + 报告可用性 20%），采用规则打底 + LLM-as-Judge 二审的混合评分管道，覆盖 5 个从 easy 到 hard 的真实故障场景（Pod OOM、上游限流级联、慢查询连接池耗尽、DNS 缓存雪崩、配置漂移内存泄漏）
- 评测结果：根因定位准确率 **100%**（5/5 场景），推理质量和报告可用性均 **满分**，综合评分 **0.91/1.0**；Agent 在 hard 级别场景（涉及 3+ 服务级联故障）中仍能正确还原完整因果链

**技术栈**: Python 3.13, LiteLLM, Pydantic, ChromaDB, sentence-transformers, Jinja2, Typer, Rich, HTTPX, concurrent.futures

---

## 按需补充的细节（面试展开用）

### 架构决策

| 决策 | 选择 | 理由 |
|---|---|---|
| LLM 接入层 | LiteLLM 统一适配 | 一套代码兼容 OpenAI/Anthropic/DeepSeek 等多供应商，无需为每家写 adapter |
| 工具执行 | ThreadPoolExecutor 并行 | 同一轮多个工具调用并发执行，结果按声明顺序恢复，既快又确定性 |
| 上下文压缩 | 语义粒度而非固定截断 | 按工具类型定制压缩单元（日志按异常簇、测试按失败用例），避免截断半段栈 |
| 记忆存储 | ChromaDB + JSON 双写 | ChromaDB 做向量索引层，JSON 做无损真相源，检索走向量、审计走文件 |
| 评测管道 | 规则 + LLM-as-Judge | 根因/信号可规则打分（快、零成本）；推理质量/报告可用性需语义判断，交给 Judge |

### 评测量化数据

| 场景 | 难度 | 综合分 | 根因 | 推理 | 报告 | 用时(s) |
|---|---|---|---|---|---|---|
| Pod OOM Kill | easy | 0.95 | 1.0 | 1.0 | 1.0 | 74 |
| 上游限流级联 | medium | 0.93 | 1.0 | 1.0 | 1.0 | 90 |
| 慢查询连接池耗尽 | medium | 0.94 | 1.0 | 1.0 | 1.0 | 84 |
| DNS 缓存雪崩 | hard | 0.85 | 1.0 | 1.0 | 1.0 | 143 |
| 配置漂移内存泄漏 | hard | 0.89 | 1.0 | 1.0 | 1.0 | 50 |
| **平均** | — | **0.91** | **1.0** | **1.0** | **1.0** | **88** |

评测模型: DeepSeek-V4-Flash
